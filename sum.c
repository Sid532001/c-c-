#include<stdio.h>
int main(int argc, char const *argv[])
{
    int i,a[10],sum,n;
    for(i=0;i<n;i++)
    {
    scanf("%d",&a[i]);
    }
    sum=sum+a[i];
    for(i=0;i<n;i++){
    printf("%d",a[i]);
    }
    return 0;
}
